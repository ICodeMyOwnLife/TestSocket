<Window x:Class="SocketServerWindow.ServerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vmd="clr-namespace:SocketServer;assembly=SocketServer"
        mc:Ignorable="d"
        Title="Server" Height="350" Width="525">
    <Window.DataContext>
        <vmd:ServerViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Content="Start Listening"
                Command="{Binding StartListeningAsyncCommand}" />

        <Button Grid.Row="0" Grid.Column="1" Content="Stop Listening"
                Command="{Binding StopListeningCommand}" />

        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                 Text="{Binding Message, Mode=OneWay}" IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>